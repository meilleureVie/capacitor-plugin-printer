import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chezgugu.capacitor.printer',
  appName: 'capacitor-plugin-printer',
  webDir: 'dist'
};

export default config;
